GET
	/get/{Int} -- return json

POST
	/addPlayer -- return json
		"name"

	/newName -- retrun json
		id -- Int id
		"name" -- new name

	/pScore/ {easy | medium | hard} -- json succes
		id -- Int id
		nScore -- Int score